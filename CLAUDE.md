# PORTAL SYSTEMS — правила проекта

Сайт PORTAL SYSTEMS: HS-порталы (подъёмно-раздвижные) и FS-порталы (складные), панорамное остекление, Москва и МО.
Статический сайт на HTML, CSS и JS без сборщика и без зависимостей. Сейчас деплой на GitHub Pages, позже переезд
на REG.RU с PHP (формы уходят через `forms/send.php`).

**Это прототип.** На страницах `noindex`, `robots.txt` закрыт (`Disallow: /`). Оставляем так до запуска —
не открывать индексацию без явной команды владельца. Список дел на запуск будет в `LAUNCH.md`.

## Правила работы

- Каждый этап — отдельный пулл-реквест в `main` с коротким описанием **на русском**.
- После каждого этапа — проверка `node tools/check-site.mjs` (см. ниже). В описании PR — что проверено.
- Сохраняй текущий визуальный стиль: тонкий Manrope (300), крупные заголовки с отрицательным трекингом,
  тонкие линии-разделители, углы без скругления, чёрный `#050505` и нейтральный светлый фон `#f5f5f4` / `#ededeb`.
  Бежевые и желтоватые фоны не использовать (решение владельца) — только нейтральные серые.
- Элементы (карточки, плитки выбора, формы, кнопки) — белые, с тенями `--sh-1` / `--sh-2` / `--sh-3` и подъёмом
  при наведении; фон секций плоский. Выбранный вариант — чёрная плитка, без радиокнопок и галочек внутри;
  где можно выбрать несколько (опции калькулятора) — плитка-переключатель с «+» / «✓».
- Размеры шрифтов и отступов — **только через токены** из `assets/css/components.css`.
  Жёсткие `px` в `font-size` не использовать. Нужен новый размер — сначала подумай, подходит ли существующий токен;
  если нет — добавь токен в `components.css`, а не число в компонент.
- Текст на экране не мельче 11 px ни на одной ширине. Это касается и подписей внутри SVG: SVG масштабируется,
  поэтому считай итоговый размер (проверка это ловит).
- Manrope подключён локально из `assets/fonts/`. Google Fonts и другие внешние CDN не подключать.
- Не редактируй руками то, что собирает генератор (страницы вариантов, карточки каталога, `sitemap.xml`) —
  правь данные или шаблон генератора и пересобирай.

## Токены (`assets/css/components.css`)

Шкала плавная: `clamp()` от 360 до 1440 px, без скачков на брейкпоинтах.

| Токен | Диапазон | Для чего |
|---|---|---|
| `--fs-label` | 11 → 12 px | КАПС-подписи, eyebrow, кнопки |
| `--fs-caption` | 12 → 13 px | сноски, мелкие пояснения |
| `--fs-small` | 13 → 15 px | описания в карточках |
| `--fs-body` | 15 → 17 px | основной текст |
| `--fs-lead` | 17 → 20 px | лид-абзацы |
| `--fs-h4` | 18 → 22 px | мелкие заголовки, значения |
| `--fs-h3` | 22 → 36 px | заголовки карточек и аккордеонов |
| `--fs-num` | 30 → 52 px | цены, крупные цифры |
| `--fs-h2s` | 32 → 56 px | подзаголовки поверх фото, H1 товара |
| `--fs-h2` | 38 → 84 px | заголовки секций |
| `--fs-h1` | 46 → 112 px | заголовок первого экрана |
| `--fs-display` | 58 → 160 px | акцидентные надписи |
| `--fs-mega` | 64 → 124 px | крупные цифры-акценты в инфоблоках (158 / 246 мм) |
| `--ui-pad` | 20 px → 4,5 % ширины | боковые поля (одни на весь сайт) |
| `--space-section` | 64 → 140 px | вертикальный ритм секций |
| `--space-head` | 28 → 56 px | от заголовка секции до контента |
| `--header-h` | 72 / 88 px (с 900 px) | высота шапки, `scroll-margin` якорей |

Цвета: `--ui-black #050505`, `--ui-ink #11110f`, `--ui-paper #ededeb`, `--ui-bg #f5f5f4`, `--ui-muted #6f6c64`,
`--ui-line #a9a8a5`, `--ui-line-soft #cfcecd`, `--ui-accent #b1a27a`, `--ui-max 1680px`.

Ширина страниц одна на весь сайт: `--ui-max` 1680 px, поля `--ui-pad` (20 px на телефоне, 4,5 % ширины на больших экранах).
Шапка, подвал, HS (`--hs-max`/`--hs-pad`), товар (`.p-wrap`) и FS берут эти же токены — не заводить свои.

Тени: `--sh-1` (покой), `--sh-2` (панели), `--sh-3` (наведение, выбранное), `--sh-btn` (тёмная кнопка при наведении), `--ease-ui`.

Общие классы там же: `.ui-wrap`, `.ui-eyebrow`, `.ui-title`, `.ui-lead`, `.ui-btn` (`--dark`, `--light`),
`.ui-section-head`, карточка товара `.product-card*` (в т. ч. `--dark`, `--soon`).

## Структура

```
index.html                  главная (калькулятор, системы, проекты, форма)
systems/hs/, systems/fs/    страницы систем HS и FS
catalog/index.html          каталог: карточки по моделям
catalog/hs-portaly/<slug>/  страницы вариантов HS (модель × цвет × схема)
catalog/fs-portaly/<slug>/  страницы вариантов FS (пока «скоро»)
cart/, cart/done/           корзина с оформлением и страница «Заказ принят»
raschet/                    калькулятор HS — одна страница на весь сайт (адрес принимает ?w=&h=&n=&glass=&color=&from=)
about/                      «О компании»: цифры, «Почему мы» (#why), производство, реквизиты (#requisites)
contacts/                   «Контакты»: телефон, часы, адрес производства, Яндекс Карты, самовывоз (#pickup), форма
data/products.json          модели, цвета, схемы, цены — источник данных каталога
data/site-config.json       домен, телефон, производство (адрес, координаты, Яндекс), доставка и монтаж (%)
assets/css/components.css   токены, шрифты, общие компоненты (подключается во все CSS через @import)
assets/css/site.css         главная и общие секции; catalog.css, product.css, cart.css, pages.css (о компании, контакты), hs-system.css, fs-system.css, calculator.css
assets/css/hs-blocks.css    HS: блоки «Характеристики и устройство» и «Как открывается дверь» (классы .hsx-*)
assets/js/components/       <site-menu> и <site-footer> — веб-компоненты шапки и подвала (Shadow DOM, атрибут data-base)
assets/js/                  calculator*.js, hs-system.js, product.js, site.js
assets/js/hs-opening.js     HS: конфигуратор «Как открывается дверь» (ширина × сторона → схема, проход, ссылка на товар)
assets/js/quick-calc.js     калькулятор HS на /raschet/ ([data-quick-calc]): цена, рендер модели, ползунок «Закрыто — Открыто», заявка с файлом
assets/css/quick-calc.css   его стили (.qc*) и анонс «Калькулятор» на главной и HS (.qc-teaser)
assets/css/raschet.css      страница /raschet/: шаги «Что дальше», готовые конфигурации
assets/js/components/lead-form.js  <lead-form> — одна форма заявки на весь сайт (стили .lead-form* в components.css)
assets/fonts/               Manrope (woff2, кириллица + латиница), OFL.txt
assets/images/              фото (webp/jpg)
forms/send.php, config.php  отправка заявок на почту (PHP mail) — работает только на REG.RU
canvas/                     черновые интерактивные схемы HS/FS (не часть сайта, в sitemap не входят)
tools/build.mjs             генератор каталога и sitemap из data/products.json
tools/check-site.mjs        проверка сайта в браузере
tools/recolor-photos.mjs    фото вариантов в цветах RAL из студийных рендеров
```

Пути в HTML относительные (`../../`), чтобы сайт работал и на GitHub Pages в подпапке, и на своём домене.
Шапка и подвал получают корень сайта через `data-base`, например `<site-menu data-base="../../"></site-menu>`.
Боевой домен — `https://www.portal-systems.ru/` (канонические адреса, sitemap).

## Генератор каталога (`tools/build.mjs`)

```
node tools/build.mjs          # собрать после любой правки data/products.json или шаблона
node tools/build.mjs --check  # ничего не пишет; код 1, если файлы устарели (запускать перед PR)
```

Источник — `data/products.json`: модели с цветами и схемами, вариант = модель × цвет × схема.
Адрес варианта: `catalog/<cat>/<профиль>-<Ш>x<В>-<цвет.slug>-<схема.slug>/`, артикул
`STD-<модель>-<профиль>-<Ш>X<В>-<RAL>-<код схемы>`. Не меняй `slug` у опубликованных цветов и схем — сменится адрес.

Генератор собирает:
- страницы всех вариантов — цена, цвет, схема, характеристики прямо в HTML; canonical на себя;
  переключатели цвета и схемы — обычные ссылки; JSON-LD `Product`/`Offer` и `BreadcrumbList`;
- блоки между `<!-- build:имя -->` и `<!-- /build:имя -->`: карточки моделей в `catalog/index.html`
  и `systems/hs/index.html`, первый экран HS, мини-карточки на главной. Остальное на этих страницах — ручная вёрстка;
- `sitemap.xml` (главные страницы + все варианты);
- `data/catalog.json` — артикулы, цены и названия для корзины.

Карточки товаров — компонент `.m-card` в `components.css` (как на маркетплейсе: фото, цена, название,
цвет и схема переключаются прямо в карточке, кнопка «В корзину»). Последняя карточка в ряду HS —
«Индивидуальный расчёт», вся карточка — ссылка на `/raschet/`.

### Калькулятор и заявки — единые правила
- Калькулятор один — страница `/raschet/` (блок `build:raschet-calc`). Это одна белая панель: сверху вкладки-шаги
  (Проём → Створки → Стекло и цвет → Опции; на телефоне пятый шаг — Итог), внутри текущий шаг, справа (с 1100 px) итог
  с фото и ценой; на телефоне снизу панели прилипает строка «цена + Далее». Плитки внутри панели плоские, с рамкой. На него ведут меню, подвал, карточка
  «Индивидуальный расчёт», кнопки «Рассчитать…», анонс «Калькулятор» на главной (`#calculator`) и HS (`#hs-calculator`,
  блоки `build:home-calc` / `build:hs-calc`: фото, ширина × высота → `/raschet/?w=&h=`). Со страницы товара — ссылка
  с параметрами товара (`raschetHref` в build.mjs).
- Прежние калькуляторы главной и HS скрыты `<div class="calc-legacy" hidden>` (id `calculator-legacy`, `hs-calculator-legacy`) —
  по решению владельца не удалять, пока не скажет.
- Форма заявки одна — `<lead-form>` (имя, телефон, задача, файл проекта, «Замер — бесплатно», ссылка на калькулятор).
  Стоит на главной, HS (`data-theme="dark"`), товаре, «Контактах». Новые формы не верстать — ставить `<lead-form>`.
- Цены калькулятора — из `products.json` (цена, ширина, высота модели HS по числу секций). Формула: цена модели ×
  площадь / площадь модели × стеклопакет (1 / 1,14 / 1,26) × цвет (белый и антрацит 1, другой RAL 1,09), вверх до 1000 ₽.
  Фото — рендеры моделей (белая рама есть только у 2-створчатой; для 3 и 4 створок показывается тёмная).

Страница товара, порядок блоков: карточка (цена + плашка «без доставки и монтажа») → 01 характеристики →
инженерная спецификация → 02 «Под ваш проём» (варианты с иконками, «Рассчитать под свой размер» → `/raschet/?…`,
`<lead-form>`, `id="product-contact"`) → «Система внутри архитектуры» (проекты) в самом низу.

## Корзина

- `assets/js/shop.js` — корзина в `localStorage` (`ps-cart`: `[{sku, qty}]`), кнопки `[data-add-to-cart]`,
  переключение цвета и схемы в карточках. Подключать на страницах с карточками и кнопками «В корзину».
- Счётчик в шапке — в `<site-menu>`, слушает событие `ps-cart-change`.
- `/cart/` — список, количество, итог, оформление (имя, телефон, город, комментарий, согласие); `assets/js/cart.js`.
  Цены берутся из `data/catalog.json`, не из хранилища. Заказ → `forms/send.php` с `source=cart`
  (номер `PS-ГГММДД-XXXX`, письмо «Заказ № …») → `/cart/done/?order=…`.
- Онлайн-оплаты нет: звонок инженера → замер → договор → счёт.
- Получение: «Доставка и монтаж» — оценка `services.install_delivery_pct` % от стоимости конструкций, не меньше
  `install_delivery_min` (сейчас 12 %, от 45 000 ₽, Москва и МО до 50 км от МКАД), или «Самовывоз с производства» (0 ₽,
  город не нужен). Значения — в `data/site-config.json`, в корзину попадают через `data/catalog.json` (пересобрать).
- На GitHub Pages PHP не работает, поэтому оформление там показывает ошибку отправки. Полностью работает на REG.RU.

## Производство и контакты

Производство и самовывоз: Москва, ТиНАО, Краснопахорский район, квартал № 205, 1, стр. 1; ежедневно 10:00–20:00
(по данным Яндекс Карт); координаты 55.404982, 37.402583; организация на Яндексе — https://yandex.ru/maps/org/okna/178905094330/.
Карта на `/contacts/` грузится только по нажатию «Показать карту» (виджет Яндекса), до этого — никаких сторонних запросов.
Адрес и телефон продублированы вручную в `about/`, `contacts/`, подвале и `site-config.json` — меняйте везде.

Папки `catalog/hs-portaly/` и `catalog/fs-portaly/` принадлежат генератору: лишние удаляются.
`status: "soon"` — страница есть, цена скрыта, в JSON-LD нет `Offer`, в фид не попадает (FS пока так).
`status: "available"` — в продаже.

Фото вариантов: `assets/images/products/<модель>/<цвет.slug>-<схема.slug>.webp` и `…-open.webp` (открытый вид,
тот же ракурс). Их делает `node tools/recolor-photos.mjs` из студийного рендера модели (поле `render` в
`products.json`: закрытый и открытый кадр и схема на нём): перекрашивает раму в RAL каждого цвета, зеркальные
схемы отражает. Потом `node tools/build.mjs`. Нет фото варианта — стоит общее фото модели (`image`);
список недостающих генератор печатает сам. На странице товара — переключатель «Закрыто / Открыто»,
в карточке открытый вид показывается при наведении. Рендеры: HS / 30, HS / 36 — закрыто + открыто,
HS / 48 — закрыто + открыто (ручки на рендерах разного цвета), FS / 36 — только закрыто (открытый кадр
не соответствует схеме 3 + 1), FS / 27 — нет рендера.
Параметры `render`: `flip` — рендер зеркален схеме (HS / 36: активные створки на рендере справа, в D1 — слева);
`align` — открытый кадр снят с другой точки, скрипт выравнивает его по четырём углам рамы закрытого.
Горизонтальные кадры дополняются фоном до 4:5, чтобы рама не обрезалась в квадратной карточке.
Проверяй схему по кадру: где ручка и куда складывается пакет — открытый кадр должен совпадать со схемой SKU.

## Проверка

```
node tools/check-site.mjs                 # все страницы из sitemap.xml на 360, 390, 768, 1280, 1920
node tools/check-site.mjs /catalog/       # отдельные страницы (корзина не в sitemap: /cart/ /cart/done/)
node tools/check-site.mjs --shots .check  # плюс скриншоты во всю страницу (папка .check в .gitignore)
```

Скрипт поднимает свой локальный сервер и открывает страницы в Chromium через Playwright.
Проверяет: ошибки в консоли и 404 локальных файлов, горизонтальную прокрутку, текст мельче 11 px
(включая подписи в SVG с учётом масштаба). Код выхода 1 — есть нарушения. Скриншоты смотри глазами:
скрипт не видит наложений и сломанной вёрстки.

## Дорожная карта

1. `CLAUDE.md` — этот файл.
2. Каталог из `data/products.json`: генератор статических страниц 16 вариантов (8 HS, 8 FS), карточки каталога
   по моделям, JSON-LD Product/Offer, `sitemap.xml`. FS — статус «скоро»: страницы есть, в фид не попадают.
3. YML-фид для Яндекс Товаров (`feed.yml`) — тем же генератором; данные совпадают со страницами.
4. Корзина и оформление заказа без онлайн-оплаты — сделано по запросу владельца (карточки как на маркетплейсе).
5. Обязательные страницы: «О компании» с реквизитами и «Контакты» — сделаны (реквизиты — заглушки `[ЗАПОЛНИТЬ]`);
   осталось: «Доставка и монтаж», «Оплата», «Возврат».
6. `LAUNCH.md` — список дел на запуск.

После этапов 2–5 **предложить, но не делать без согласия**: перестройку мобильной страницы HS
(12 экранов → ~7) и два действия на всём сайте: «Выбрать готовый портал» и «Рассчитать под свой проём».

## Известные проблемы

- PDF политики конфиденциальности (`content/privacy-policy-portal-systems.pdf`) отсутствует — ссылка в формах битая.
- `index.html` и `systems/*` помечены `index,follow`, остальные — `noindex`; до запуска всё закрыто `robots.txt`.
- Почта в `forms/config.php` не заполнена.
- `forms/send.php` принимает файл проекта (`project_file`: PDF, DWG, DXF, JPG, PNG, WEBP, HEIC, ZIP до 15 МБ) и шлёт его вложением.
  На REG.RU проверить `upload_max_filesize` ≥ 16M и `post_max_size` ≥ 20M, иначе большие файлы не дойдут.
- Скрытый прежний калькулятор главной (`calculator.js`) считает антрацит на 4 % дороже белого; каталог и `/raschet/` — по одной цене.
- Фото в блоках «Стеклопакеты» и «Как работает» на странице HS (`assets/images/systems/hs-glass-44.webp`,
  `hs-mechanism.webp`, `hs-steps/`) вырезаны из макетов и увеличены — заменить исходными рендерами без подписей
  (ширина от 1600 px, шаги от 400 px), имена файлов оставить.
- Справочные цены калькулятора HS заданы в `assets/js/hs-system.js` (`refs`) отдельно от каталога —
  генератор предупреждает, если они разошлись с `products.json`. Конфигуратор «Как открывается дверь»
  (`hs-opening.js`) показывает HS/36 слева и справа, а в каталоге у HS/36 одна схема — обе стороны ведут на неё.
