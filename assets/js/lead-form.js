(()=>{document.querySelectorAll('[data-lead-form]').forEach(form=>{const status=form.querySelector('.form-status');form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form);const phone=String(fd.get('phone')||'').trim();if(phone.replace(/\D/g,'').length<10){if(status)status.textContent='Проверьте номер телефона.';return;}if(!form.querySelector('[name=\"privacy_consent\"]:checked')){if(status)status.textContent='Подтвердите согласие с политикой конфиденциальности.';return;}if(status)status.textContent='Демо-режим: форма проверена, внешняя отправка отключена.';});});})();
