(() => {
 const root=document.querySelector('[data-hs-canvas]');if(!root)return;
 const canvas=root.querySelector('#hsSystemCanvas'),stage=root.querySelector('.hs-canvas-stage'),toggle=root.querySelector('[data-hs-toggle]'),state=root.querySelector('[data-hs-state]'),ctx=canvas?.getContext('2d',{alpha:true});if(!canvas||!ctx||!stage||!toggle||!state)return;
 const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,clamp=v=>Math.max(0,Math.min(1,v)),ease=t=>1-Math.pow(1-t,3),lerp=(a,b,t)=>a+(b-a)*t;let p=0,target=0,raf=0,drag=false;
 const resize=()=>{const r=canvas.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.max(1,Math.round(r.width*dpr));canvas.height=Math.max(1,Math.round(r.height*dpr));ctx.setTransform(dpr,0,0,dpr,0,0);draw()};
 const glass=(x,y,w,h,frame)=>{const g=ctx.createLinearGradient(x,y,x+w,y+h);g.addColorStop(0,'rgba(249,251,251,.72)');g.addColorStop(.5,'rgba(205,216,216,.22)');g.addColorStop(1,'rgba(246,248,247,.58)');ctx.fillStyle=g;ctx.fillRect(x,y,w,h);ctx.strokeStyle='#1b1e1f';ctx.lineWidth=frame;ctx.strokeRect(x,y,w,h)};
 function draw(){const w=canvas.clientWidth,h=canvas.clientHeight;if(!w||!h)return;ctx.clearRect(0,0,w,h);const bg=ctx.createLinearGradient(0,0,0,h);bg.addColorStop(0,'#f1f0ed');bg.addColorStop(1,'#ddd9d0');ctx.fillStyle=bg;ctx.fillRect(0,0,w,h);const m=w<640,left=w*.06,right=w-left,top=h*.14,bottom=h*.8,ow=right-left,oh=bottom-top,fw=m?6:9;ctx.strokeStyle='#202323';ctx.lineWidth=fw;ctx.strokeRect(left,top,ow,oh);const leaf=ow*.52,fixed=ow*.48,travel=ow-leaf-fw*.7,lift=Math.min(1,p/.16),slide=p<=.16?0:(p-.16)/.84,liftY=lerp(0,m?-5:-8,ease(lift)),paneH=oh-(m?10:14),fixedX=right-fixed-fw*.4;glass(fixedX,top+(m?5:7),fixed-(m?5:7),paneH,m?5:6);if(p>.01){ctx.fillStyle='rgba(245,244,239,.94)';ctx.fillRect(left+fw,top+fw,travel*ease(slide),oh-fw*2)}const ax=left+fw+travel*ease(slide),ay=top+fw+liftY,aw=leaf-fw*.35;glass(ax,ay,aw,paneH,m?6:7);ctx.fillStyle='#151717';ctx.fillRect(ax+12,ay+paneH*.46,3,m?30:38);ctx.strokeStyle='rgba(17,17,17,.25)';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(left,bottom+8);ctx.lineTo(right,bottom+8);ctx.stroke();state.textContent=p<.02?'Закрыто':p>.98?'Открыто':Math.round(p*100)+'%';toggle.textContent=p>.5?'Закрыть':'Открыть'}
 function animate(){cancelAnimationFrame(raf);if(reduce){p=target;draw();return}const step=()=>{p+=(target-p)*.11;if(Math.abs(target-p)<.002){p=target;draw();return}draw();raf=requestAnimationFrame(step)};step()}
 toggle.addEventListener('click',()=>{target=p>.5?0:1;animate()});stage.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&e.button!==0)return;drag=true;stage.setPointerCapture?.(e.pointerId);cancelAnimationFrame(raf)});stage.addEventListener('pointermove',e=>{if(!drag)return;const r=stage.getBoundingClientRect();p=clamp((e.clientX-r.left)/r.width);target=p;draw()});const finish=e=>{if(!drag)return;drag=false;target=p>.45?1:0;animate();try{stage.releasePointerCapture?.(e.pointerId)}catch(_){}};stage.addEventListener('pointerup',finish);stage.addEventListener('pointercancel',finish);if('ResizeObserver'in window)new ResizeObserver(resize).observe(canvas);else addEventListener('resize',resize,{passive:true});resize();
})();