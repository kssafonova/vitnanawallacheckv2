(()=>{
  const items=[...document.querySelectorAll('.sysx-item')];
  items.forEach(item=>{const head=item.querySelector('.sysx-head');head?.addEventListener('click',()=>{const opening=!item.classList.contains('is-open');items.forEach(x=>{x.classList.remove('is-open');x.querySelector('.sysx-head')?.setAttribute('aria-expanded','false')});if(opening){item.classList.add('is-open');head.setAttribute('aria-expanded','true');if(item.dataset.sysx==='fs')requestAnimationFrame(resizeFs)}})});
  const hr=document.getElementById('hsRange'), sash=document.getElementById('hsMovingSash'), label=document.getElementById('hsStateLabel'), steps=[...document.querySelectorAll('#hsSteps .sysx-motion-step')];
  function renderHs(){if(!hr||!sash)return;const p=+hr.value/100,l=Math.max(0,Math.min(1,(p-.33)/.27)),s=Math.max(0,Math.min(1,(p-.60)/.40));sash.style.transform=`translate3d(${-82*s}%,${-1.5*l}%,0) scaleY(${1-.012*l})`;label.textContent=p<.02?'Закрыто':p>.98?'Открыто':Math.round(p*100)+'%';const a=p<.34?0:p<.64?1:2;steps.forEach((n,i)=>n.classList.toggle('is-active',i===a))}hr?.addEventListener('input',renderHs);renderHs();
  const hm=document.getElementById('hsMedia');let hd=false;function hp(e){const r=hm.getBoundingClientRect();hr.value=Math.round(Math.max(0,Math.min(1,(e.clientX-r.left)/r.width))*100);renderHs()}hm?.addEventListener('pointerdown',e=>{hd=true;hm.setPointerCapture(e.pointerId);hp(e)});hm?.addEventListener('pointermove',e=>{if(hd)hp(e)});hm?.addEventListener('pointerup',()=>hd=false);hm?.addEventListener('pointercancel',()=>hd=false);
  const c=document.getElementById('fsCanvas'),st=document.getElementById('fsStage'),fr=document.getElementById('fsRange'),fs=document.getElementById('fsState');if(!c||!st||!fr)return;const ctx=c.getContext('2d');let fp=0,drag=false;const clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),lerp=(a,b,t)=>a+(b-a)*t,ease=t=>1-Math.pow(1-t,3);function poly(ps,fill,stroke,lw){ctx.beginPath();ctx.moveTo(ps[0].x,ps[0].y);ps.slice(1).forEach(p=>ctx.lineTo(p.x,p.y));ctx.closePath();if(fill){ctx.fillStyle=fill;ctx.fill()}if(stroke){ctx.strokeStyle=stroke;ctx.lineWidth=lw;ctx.stroke()}}
  function resizeFs(){if(!c.offsetParent)return;const r=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);c.width=Math.round(r.width*d);c.height=Math.round(r.height*d);ctx.setTransform(d,0,0,d,0,0);drawFs()}
  function drawFs(){const w=c.clientWidth,h=c.clientHeight;if(!w||!h)return;ctx.clearRect(0,0,w,h);ctx.fillStyle='#0d0d0d';ctx.fillRect(0,0,w,h);const mob=w<640,mx=w*(mob?.06:.075),top=h*.12,bottom=h*.82,left=mx,right=w-mx,ow=right-left,q=ease(fp),pw=ow/6,fw=Math.max(12,ow*(mob?.035:.026));ctx.strokeStyle='rgba(255,255,255,.82)';ctx.lineWidth=mob?5:7;ctx.strokeRect(left,top,ow,bottom-top);for(let i=5;i>=0;i--){const x=lerp(left+i*pw,left+i*fw,q),th=Math.min(1,fp*1.08)*(Math.PI/2)*.91,aw=Math.max(fw,lerp(pw,fw,q)*Math.cos(th)),dep=Math.sin(th)*pw*(mob?.2:.26)*(i%2===0?1:-1);poly([{x,y:top+6},{x:x+aw,y:top+6+dep},{x:x+aw,y:bottom-6-dep*.14},{x,y:bottom-6}],'rgba(190,210,216,.08)','rgba(255,255,255,.86)',mob?3:4)}const pct=Math.round(fp*100);fs.textContent=pct<=1?'Закрыто':pct>=99?'Открыто':pct+'%'}function set(v){fp=clamp(v,0,1);fr.value=Math.round(fp*100);drawFs()}fr.addEventListener('input',()=>set(+fr.value/100));function ptr(e){const r=st.getBoundingClientRect();set((e.clientX-r.left)/r.width)}st.addEventListener('pointerdown',e=>{drag=true;st.setPointerCapture(e.pointerId);ptr(e)});st.addEventListener('pointermove',e=>{if(drag)ptr(e)});st.addEventListener('pointerup',()=>drag=false);st.addEventListener('pointercancel',()=>drag=false);addEventListener('resize',()=>requestAnimationFrame(resizeFs));requestAnimationFrame(resizeFs);
})();
