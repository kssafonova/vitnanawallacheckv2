class LeadForm extends HTMLElement {
 connectedCallback(){const label=this.getAttribute('button')||'Получить расчёт';this.innerHTML=`<form class="contact-form lead-form-component" novalidate><input aria-hidden="true" autocomplete="off" name="website" tabindex="-1" type="text" class="hp-field"><label class="field"><span>Имя</span><input autocomplete="name" name="name" placeholder="Ваше имя" required></label><label class="field"><span>Телефон</span><input autocomplete="tel" inputmode="tel" name="phone" placeholder="+7 999 000-00-00" required></label><button class="btn" type="submit">${label} <span>→</span></button><p class="form-status" aria-live="polite"></p></form>`;const form=this.querySelector('form'),status=this.querySelector('.form-status');form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form),name=(fd.get('name')||'').toString().trim(),phone=(fd.get('phone')||'').toString().replace(/\D/g,'');if(!name||phone.length<10){status.textContent='Проверьте имя и телефон.';return}status.textContent='Демо-режим: форма готова к подключению отправки.';});}}
customElements.define('lead-form',LeadForm);